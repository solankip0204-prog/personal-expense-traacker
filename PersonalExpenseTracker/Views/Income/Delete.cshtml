@model Income
@{
    ViewData["Title"] = "Delete Income";
}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card p-4 border-danger">
                <h4 class="fw-bold mb-3 text-danger"><i class="bi bi-exclamation-triangle me-1"></i>Delete Income</h4>
                <p>Are you sure you want to delete this income record? This action cannot be undone.</p>
                <dl class="row mb-0">
                    <dt class="col-sm-4">Amount</dt><dd class="col-sm-8">₹@Model.Amount.ToString("N2")</dd>
                    <dt class="col-sm-4">Source</dt><dd class="col-sm-8">@Model.Source</dd>
                    <dt class="col-sm-4">Date</dt><dd class="col-sm-8">@Model.IncomeDate.ToString("dd MMM yyyy")</dd>
                </dl>
                <form asp-action="Delete" asp-route-id="@Model.IncomeId" method="post" class="mt-3">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-danger"><i class="bi bi-trash me-1"></i>Confirm Delete</button>
                    <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>
