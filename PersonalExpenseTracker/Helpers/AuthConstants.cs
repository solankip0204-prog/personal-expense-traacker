namespace PersonalExpenseTracker.Helpers
{
    // Role name constants used with [Authorize(Roles = ...)].
    public static class AuthConstants
    {
        public const string RoleUser = "User";
        public const string RoleAdmin = "Admin";
    }
}
